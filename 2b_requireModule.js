var response = require('./2a_requireModule.js'); // Put the directory sign so that node knows that it is a local module
//if there is another module in global given by node then it may be mixed up
console.log(response);
