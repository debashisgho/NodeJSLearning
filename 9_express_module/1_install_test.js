//Run the following to install express
//npm install express --save
//--save will install node as a folder in your work folder and create a node_module package

//Now test if express is working. 

var express = require('express');

//if it does not throw error it means it is working